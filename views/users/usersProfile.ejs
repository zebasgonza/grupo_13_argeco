<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
        <link rel="stylesheet" href="/css/usersProfile.css">
        <link rel="stylesheet" href="/css/index.css">
</head>

<body>
    <%- include('../partials/header')%>
        <main>
            <div class="general">
                <h2>Perfil de Usuario</h2>
            </div>
            <div class="sub-titulos"> 
                Mis Datos
            </div>

            <div class="sub-titulos">Datos de cuenta</div>
            <img class="imagen-usuario" src=<%= user.image%> alt="foto usuario">
            <div class="datosPersonales">
                <div>Usuario:</div>
                <div>Email:</div>
            </div>
            <div class="sub-titulos">Datos Personales</div>
            <div class="datosPersonales">
                <div>id:<%= user.id %></div>
                <div>Nombre:<%= user.first_name %></div>
                <div>Apellido: <%= user.last_name %></div>
                <div>Email: <%= user.email  %></div>
                <div>Categoria <%= user.categoria  %></div>
            </div>
            <form action= "/users/delete/<%= user.id %>?_method=DELETE" method="post" class="datosPersonales">
                <button type="submit">Eliminar</button>
            </form>
        </main>

</body>
<%- include('../partials/footer') %>

</html>