<!DOCTYPE html>
<html lang="en">

<head>
    <%- include('../partials/head') %>
        <link rel="stylesheet" href="/css/usersProfile.css">
        <link rel="stylesheet" href="/css/index.css">
</head>

<body>
    <%- include('../partials/header')%>
        <main>
            <div class="general">
                <h2>Perfil de Usuario</h2>
            </div>
            <div class="sub-titulos"> 
                Mis Datos
            </div>

            <div class="sub-titulos">Datos de cuenta</div>

            <img class="imagen-usuario" src="/img/users/<%= user.image %>" alt="foto usuario">

            <div class="sub-titulos">Datos Personales</div>
            <div class="datosPersonales">
                <div>id:<%= user.id_usuario %></div>
                <div>Nombre:<%= user.nombre %></div>
                <div>Apellido: <%= user.apellido %></div>
                <div>Email: <%= user.email  %></div>
                <div>Categoria <%= user.categoria  %></div>
            </div>
            <form action= "/users/delete/<%= user.id_usuario %>?_method=DELETE" method="post" class="datosPersonales">
                <button type="submit">Eliminar</button>
            </form>
        </main>

</body>
<%- include('../partials/footer') %>

</html>